server:
  port: 8888


spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/parisaaabdollahi/config-server.git
          clone-on-start: true

    compatibility-verifier:
      enabled: false

  application:
    name: config-server